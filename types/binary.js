module.exports = {
  "binary.base": "{{#label}} 必须为二进制",
  "binary.length": "{{#label}} 大小必须为{{#limit}}",
  "binary.max": "{{#label}} 大小不成超过{{#limit}}字节",
  "binary.min": "{{#label}} 大小不能少于{{#limit}}字节",
};
